<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cc.endmc.system.mapper.SysNoticeMapper">
    
    <resultMap type="SysNotice" id="SysNoticeResult">
        <result property="noticeId"       column="notice_id"       />
        <result property="noticeTitle"    column="notice_title"    />
        <result property="noticeType"     column="notice_type"     />
        <result property="noticeContent"  column="notice_content"  />
        <result property="status"         column="status"          />
        <result property="typeColor"      column="type_color"      />
        <result property="effectiveStartTime" column="effective_start_time" />
        <result property="effectiveEndTime" column="effective_end_time" />
        <result property="showInFrontend" column="show_in_frontend" />
        <result property="isPinned"       column="is_pinned"       />
        <result property="createBy"       column="create_by"       />
        <result property="createTime"     column="create_time"     />
        <result property="updateBy"       column="update_by"       />
        <result property="updateTime"     column="update_time"     />
        <result property="remark"         column="remark"          />
    </resultMap>
    
    <sql id="selectNoticeVo">
                select notice_id, notice_title, notice_type, cast(notice_content as char) as notice_content, status, type_color, effective_start_time, effective_end_time, show_in_frontend, is_pinned, create_by, create_time, update_by, update_time, remark 
		from sys_notice
    </sql>
    
    <select id="selectNoticeById" parameterType="Long" resultMap="SysNoticeResult">
        <include refid="selectNoticeVo"/>
        where notice_id = #{noticeId}
    </select>
    
    <select id="selectNoticeList" parameterType="SysNotice" resultMap="SysNoticeResult">
        <include refid="selectNoticeVo"/>
        <where>
			<if test="noticeTitle != null and noticeTitle != ''">
				AND notice_title like concat('%', #{noticeTitle}, '%')
			</if>
			<if test="noticeType != null and noticeType != ''">
				AND notice_type = #{noticeType}
			</if>
			<if test="createBy != null and createBy != ''">
				AND create_by like concat('%', #{createBy}, '%')
			</if>
			<if test="status != null and status != ''">
				AND status = #{status}
			</if>
            <if test="showInFrontend != null and showInFrontend != ''">
                <choose>
                    <when test="showInFrontend == '1'">
                        AND (show_in_frontend = '1' OR show_in_frontend = 'Y' OR show_in_frontend = 'y')
                    </when>
                    <when test="showInFrontend == '0'">
                        AND (show_in_frontend = '0' OR show_in_frontend = 'N' OR show_in_frontend = 'n')
                    </when>
                    <otherwise>
                        AND show_in_frontend = #{showInFrontend}
                    </otherwise>
                </choose>
            </if>
		</where>
                order by create_time desc
    </select>

        <select id="selectActiveFrontendNoticeList" resultMap="SysNoticeResult">
                <include refid="selectNoticeVo"/>
                where status = '0'
                    and (show_in_frontend is null or show_in_frontend = '' or show_in_frontend = '1' or show_in_frontend = 'Y' or show_in_frontend = 'y')
                    and (effective_start_time is null or effective_start_time &lt;= now())
                    and (effective_end_time is null or effective_end_time &gt;= now())
                order by case when is_pinned = '1' or is_pinned = 'Y' or is_pinned = 'y' then 1 else 0 end desc,
                         effective_start_time desc,
                         create_time desc
                <if test="limit != null and limit &gt; 0">
                        limit #{limit}
                </if>
        </select>
    
    <insert id="insertNotice" parameterType="SysNotice">
        insert into sys_notice (
			<if test="noticeTitle != null and noticeTitle != '' ">notice_title, </if>
			<if test="noticeType != null and noticeType != '' ">notice_type, </if>
			<if test="noticeContent != null and noticeContent != '' ">notice_content, </if>
			<if test="status != null and status != '' ">status, </if>
            <if test="typeColor != null and typeColor != '' ">type_color, </if>
            <if test="effectiveStartTime != null">effective_start_time, </if>
            <if test="effectiveEndTime != null">effective_end_time, </if>
            <if test="showInFrontend != null and showInFrontend != '' ">show_in_frontend, </if>
            <if test="isPinned != null and isPinned != '' ">is_pinned, </if>
			<if test="remark != null and remark != ''">remark,</if>
 			<if test="createBy != null and createBy != ''">create_by,</if>
 			create_time
 		)values(
			<if test="noticeTitle != null and noticeTitle != ''">#{noticeTitle}, </if>
			<if test="noticeType != null and noticeType != ''">#{noticeType}, </if>
			<if test="noticeContent != null and noticeContent != ''">#{noticeContent}, </if>
			<if test="status != null and status != ''">#{status}, </if>
            <if test="typeColor != null and typeColor != ''">#{typeColor}, </if>
            <if test="effectiveStartTime != null">#{effectiveStartTime}, </if>
            <if test="effectiveEndTime != null">#{effectiveEndTime}, </if>
            <if test="showInFrontend != null and showInFrontend != ''">#{showInFrontend}, </if>
            <if test="isPinned != null and isPinned != ''">#{isPinned}, </if>
			<if test="remark != null and remark != ''">#{remark},</if>
 			<if test="createBy != null and createBy != ''">#{createBy},</if>
 			sysdate()
		)
    </insert>
	 
    <update id="updateNotice" parameterType="SysNotice">
        update sys_notice 
        <set>
            <if test="noticeTitle != null and noticeTitle != ''">notice_title = #{noticeTitle}, </if>
            <if test="noticeType != null and noticeType != ''">notice_type = #{noticeType}, </if>
            <if test="noticeContent != null">notice_content = #{noticeContent}, </if>
            <if test="status != null and status != ''">status = #{status}, </if>
            type_color = #{typeColor},
            effective_start_time = #{effectiveStartTime},
            effective_end_time = #{effectiveEndTime},
            <if test="showInFrontend != null and showInFrontend != ''">show_in_frontend = #{showInFrontend}, </if>
            <if test="isPinned != null and isPinned != ''">is_pinned = #{isPinned}, </if>
            <if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
 			update_time = sysdate()
        </set>
        where notice_id = #{noticeId}
    </update>
	
    <delete id="deleteNoticeById" parameterType="Long">
        delete from sys_notice where notice_id = #{noticeId}
    </delete>
    
    <delete id="deleteNoticeByIds" parameterType="Long">
        delete from sys_notice where notice_id in 
        <foreach item="noticeId" collection="array" open="(" separator="," close=")">
            #{noticeId}
        </foreach>
    </delete>
    
</mapper>